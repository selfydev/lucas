import {
  AnimatePresence,
  LayoutGroup,
  MotionConfig,
  MotionConfigContext,
  motion,
  useAnimationFrame,
  useInView,
  useIsPresent,
  useIsomorphicLayoutEffect,
  useMotionValue,
  usePageInView,
  useReducedMotion,
  useSpring,
  useTransform
} from "./chunk-UCSOFWH7.js";
import {
  addScaleCorrector,
  animate,
  delay,
  easeOut,
  frame,
  invariant,
  isHTMLElement,
  mix,
  motionValue,
  noop,
  pipe,
  resize,
  wrap
} from "./chunk-KF5WV3CY.js";
import {
  require_react_dom
} from "./chunk-WLGNA3JG.js";
import {
  require_jsx_runtime
} from "./chunk-YUO7M6U2.js";
import {
  require_react
} from "./chunk-KVA45Y42.js";
import {
  __toESM
} from "./chunk-V4OQ3NZ2.js";

// ../../node_modules/.bun/motion-plus-react@1.5.4+51d54415590cf6f3/node_modules/motion-plus-react/dist/es/components/Cursor/index.mjs
var import_jsx_runtime = __toESM(require_jsx_runtime(), 1);

// ../../node_modules/.bun/motion-plus-react@1.5.4+51d54415590cf6f3/node_modules/motion-plus-react/dist/es/components/Cursor/get-cursor-size.mjs
var DEFAULT_SIZE = 17;
var POINTER_SIZE = 31;
var TEXT_WIDTH = 4;
var TEXT_HEIGHT = 20;
function withDefaultSize(width, height, style) {
  return {
    width: style?.width ?? width,
    height: style?.height ?? height
  };
}
function getCursorSize({ type, state: state2, hasChildren, style, isMagnetic, magneticOptions, matchTextSize }) {
  const shouldMatchTargetSize = isMagnetic && state2.targetBoundingBox;
  if (hasChildren && !shouldMatchTargetSize) {
    return withDefaultSize("auto", "auto", style);
  }
  switch (type) {
    case "pointer":
      const { padding, morph } = magneticOptions;
      if (isMagnetic && morph && state2.targetBoundingBox) {
        const { width, height } = state2.targetBoundingBox;
        return {
          width: width + padding * 2,
          height: height + padding * 2
        };
      }
      return withDefaultSize(POINTER_SIZE, POINTER_SIZE, style);
    case "text":
      if (matchTextSize && state2.fontSize) {
        return { width: TEXT_WIDTH, height: state2.fontSize };
      }
      return withDefaultSize(TEXT_WIDTH, TEXT_HEIGHT, style);
    default:
      return withDefaultSize(DEFAULT_SIZE, DEFAULT_SIZE, style);
  }
}

// ../../node_modules/.bun/motion-plus-react@1.5.4+51d54415590cf6f3/node_modules/motion-plus-react/dist/es/components/Cursor/hooks/use-body-portal.mjs
var import_react = __toESM(require_react(), 1);
var import_react_dom = __toESM(require_react_dom(), 1);
function useBodyPortal(children) {
  const [hostElement, setHostElement] = (0, import_react.useState)(null);
  (0, import_react.useLayoutEffect)(() => {
    setHostElement(document.body);
  }, []);
  return hostElement ? (0, import_react_dom.createPortal)(children, hostElement) : null;
}

// ../../node_modules/.bun/motion-plus-react@1.5.4+51d54415590cf6f3/node_modules/motion-plus-react/dist/es/components/Cursor/hooks/use-cursor-in-view.mjs
var import_react2 = __toESM(require_react(), 1);
var listener = null;
function startInViewListener() {
  const listeners = /* @__PURE__ */ new Set();
  function showCursor() {
    listeners.forEach((listener3) => listener3.show());
  }
  function hideCursor() {
    listeners.forEach((listener3) => listener3.hide());
  }
  function addEventListeners() {
    document.body.addEventListener("mouseenter", showCursor);
    document.body.addEventListener("mouseleave", hideCursor);
  }
  function removeEventListeners() {
    document.body.removeEventListener("mouseenter", showCursor);
    document.body.removeEventListener("mouseleave", hideCursor);
  }
  return {
    on: (callbacks) => {
      if (!listeners.size) {
        addEventListeners();
      }
      listeners.add(callbacks);
      return () => {
        listeners.delete(callbacks);
        if (listeners.size === 0) {
          removeEventListeners();
        }
      };
    }
  };
}
function getInViewListener() {
  if (!listener) {
    listener = startInViewListener();
  }
  return listener;
}
function useCursorIsInView(resetSpring) {
  const [isVisible, setIsVisible] = (0, import_react2.useState)(true);
  (0, import_react2.useEffect)(() => {
    return getInViewListener().on({
      show: () => {
        if (isVisible)
          return;
        resetSpring();
        setIsVisible(true);
      },
      hide: () => setIsVisible(false)
    });
  }, [isVisible]);
  return isVisible;
}

// ../../node_modules/.bun/motion-plus-react@1.5.4+51d54415590cf6f3/node_modules/motion-plus-react/dist/es/components/Cursor/hooks/use-cursor-state/index.mjs
var import_react3 = __toESM(require_react(), 1);

// ../../node_modules/.bun/motion-plus-react@1.5.4+51d54415590cf6f3/node_modules/motion-plus-react/dist/es/components/Cursor/hooks/utils/only-mouse-events.mjs
function onlyMouse(callback) {
  return (event) => {
    if (event.pointerType === "mouse") {
      callback(event);
    }
  };
}
function onlyLeftClicks(callback) {
  return onlyMouse((event) => {
    if (event.button === 0) {
      callback(event);
    }
  });
}

// ../../node_modules/.bun/motion-plus-react@1.5.4+51d54415590cf6f3/node_modules/motion-plus-react/dist/es/components/Cursor/hooks/use-cursor-state/find-target.mjs
function findCustomTarget(target) {
  return target.closest("[data-cursor]");
}
function findPointerTarget(element) {
  return element.closest('a, button, input[type="button"]:not(:disabled)');
}
function findTextTarget(element) {
  if (window.getComputedStyle(element).userSelect === "none")
    return null;
  return element.closest("p, textarea:not(:disabled), input[type='text']:not(:disabled), h1, h2, h3, h4, h5, h6");
}
function findTarget(eventTarget) {
  let target = findCustomTarget(eventTarget);
  if (target)
    return [target.dataset.cursor, target];
  target = findPointerTarget(eventTarget);
  if (target)
    return ["pointer", target];
  target = findTextTarget(eventTarget);
  if (target)
    return ["text", target];
  return ["default", null];
}

// ../../node_modules/.bun/motion-plus-react@1.5.4+51d54415590cf6f3/node_modules/motion-plus-react/dist/es/components/Cursor/hooks/use-cursor-state/index.mjs
var listener2 = null;
var state = {
  type: "default",
  isPressed: false,
  fontSize: null,
  targetBoundingBox: null,
  target: null,
  zone: null
};
function getZone(target) {
  let zone = null;
  if ("closest" in target) {
    const zoneElement = target.closest("[data-cursor-zone]");
    if (zoneElement instanceof HTMLElement) {
      zone = zoneElement.dataset.cursorZone || null;
    }
  }
  return zone;
}
function startPointerOverListener() {
  const listeners = /* @__PURE__ */ new Set();
  function updateState(newState) {
    state = { ...state, ...newState };
    listeners.forEach((listener3) => listener3(state));
  }
  const handlePointerDown = onlyLeftClicks(() => {
    if (!state.isPressed) {
      updateState({ isPressed: true });
    }
  });
  const handlePointerUp = onlyLeftClicks(() => {
    if (state.isPressed) {
      updateState({ isPressed: false });
    }
  });
  const handlePointerOver = onlyMouse(({ target }) => {
    if (!target)
      return;
    const [type, cursorTarget] = findTarget(target);
    let hasUpdated = false;
    let newState = {
      target: cursorTarget,
      zone: getZone(target)
    };
    if (type !== state.type) {
      newState.type = type;
      hasUpdated = true;
    }
    if (newState.zone !== state.zone) {
      newState.zone = newState.zone;
      hasUpdated = true;
    }
    const size = type === "pointer" && cursorTarget ? cursorTarget.getBoundingClientRect() : null;
    if (size !== state.targetBoundingBox) {
      newState.targetBoundingBox = size;
      hasUpdated = true;
    }
    if (type === "text") {
      const { fontSize: computedFontSize } = window.getComputedStyle(target);
      const newFontSize = computedFontSize ? parseInt(computedFontSize) : null;
      if (newFontSize !== state.fontSize) {
        newState.fontSize = newFontSize;
        hasUpdated = true;
      }
    } else {
      if (state.fontSize) {
        newState.fontSize = null;
        hasUpdated = true;
      }
    }
    if (hasUpdated) {
      updateState(newState);
    }
  });
  function addEventListeners() {
    window.addEventListener("pointerover", handlePointerOver);
    window.addEventListener("pointerdown", handlePointerDown);
    window.addEventListener("pointerup", handlePointerUp);
  }
  function removeEventListeners() {
    window.removeEventListener("pointerover", handlePointerOver);
    window.removeEventListener("pointerdown", handlePointerDown);
    window.removeEventListener("pointerup", handlePointerUp);
  }
  return {
    onChange: (callback) => {
      if (!listeners.size) {
        addEventListeners();
      }
      listeners.add(callback);
      return () => {
        listeners.delete(callback);
        if (!listeners.size) {
          removeEventListeners();
        }
      };
    }
  };
}
function getPointerOverListener() {
  if (!listener2) {
    listener2 = startPointerOverListener();
  }
  return listener2;
}
function useCursorState() {
  const [latestState, setLatestState] = (0, import_react3.useState)({ ...state });
  (0, import_react3.useEffect)(() => {
    return getPointerOverListener().onChange((newState) => {
      setLatestState(newState);
    });
  }, [latestState]);
  return latestState;
}

// ../../node_modules/.bun/motion-plus-react@1.5.4+51d54415590cf6f3/node_modules/motion-plus-react/dist/es/components/Cursor/hooks/use-has-pointer-moved.mjs
var import_react5 = __toESM(require_react(), 1);
function useHasPointerMoved({ x: x2, y: y2 }, resetSpring) {
  const [pointerHasMoved, setPointerHasMoved] = (0, import_react5.useState)(x2.prev !== void 0 || y2.prev !== void 0);
  (0, import_react5.useInsertionEffect)(() => {
    if (pointerHasMoved)
      return;
    const setHasMoved = () => {
      setPointerHasMoved(true);
      resetSpring();
      stop();
    };
    const stop = pipe(x2.on("change", setHasMoved), y2.on("change", setHasMoved));
    return () => stop();
  }, [x2, y2, pointerHasMoved]);
  return pointerHasMoved;
}

// ../../node_modules/.bun/motion-plus-react@1.5.4+51d54415590cf6f3/node_modules/motion-plus-react/dist/es/components/Cursor/hooks/use-offset.mjs
function useOffset(position, offset) {
  return {
    x: useTransform(() => position.x.get() + offset.x),
    y: useTransform(() => position.y.get() + offset.y)
  };
}

// ../../node_modules/.bun/motion-plus-react@1.5.4+51d54415590cf6f3/node_modules/motion-plus-react/dist/es/components/Cursor/hooks/use-pointer-position.mjs
var x;
var y;
function initPointerTracking() {
  x = motionValue(0);
  y = motionValue(0);
  let pointerX = 0;
  let pointerY = 0;
  function updatePointer() {
    x.set(pointerX);
    y.set(pointerY);
  }
  if (typeof window !== "undefined") {
    window.addEventListener("pointermove", onlyMouse((event) => {
      pointerX = event.clientX;
      pointerY = event.clientY;
      frame.update(updatePointer);
    }));
  }
}
function usePointerPosition() {
  if (!x)
    initPointerTracking();
  return { x, y };
}

// ../../node_modules/.bun/motion-plus-react@1.5.4+51d54415590cf6f3/node_modules/motion-plus-react/dist/es/components/Cursor/hooks/use-snapped-value.mjs
var import_react10 = __toESM(require_react(), 1);

// ../../node_modules/.bun/motion-plus-react@1.5.4+51d54415590cf6f3/node_modules/motion-plus-react/dist/es/components/Cursor/hooks/use-mixed.mjs
function useMixed(a, b, p) {
  return useTransform(() => mix(a.get(), b.get(), p.get()));
}

// ../../node_modules/.bun/motion-plus-react@1.5.4+51d54415590cf6f3/node_modules/motion-plus-react/dist/es/components/Cursor/hooks/use-snapped-value.mjs
function useSnappedValue(pointer, snap, target) {
  const snapped = useSpring(0, {
    stiffness: 600,
    damping: 50
  });
  const crossfade = useMotionValue(0);
  const mixed = useMixed(pointer, snapped, crossfade);
  const prevTarget = (0, import_react10.useRef)(target);
  useIsomorphicLayoutEffect(() => {
    if (target) {
      if (!prevTarget.current && !crossfade.isAnimating()) {
        snapped.jump(target);
      } else {
        snapped.set(target);
      }
      animate(crossfade, snap);
    } else {
      animate(crossfade, 0);
    }
    prevTarget.current = target;
  }, [target]);
  return mixed;
}

// ../../node_modules/.bun/motion-plus-react@1.5.4+51d54415590cf6f3/node_modules/motion-plus-react/dist/es/components/Cursor/magnetic/use-magnetic-target.mjs
function useMagneticTarget(pointer, isEnabled, state2, snap) {
  const x2 = useSnappedValue(pointer.x, snap, state2.targetBoundingBox ? state2.targetBoundingBox.left + state2.targetBoundingBox.width / 2 : void 0);
  const y2 = useSnappedValue(pointer.y, snap, state2.targetBoundingBox ? state2.targetBoundingBox.top + state2.targetBoundingBox.height / 2 : void 0);
  return isEnabled ? { x: x2, y: y2 } : pointer;
}

// ../../node_modules/.bun/motion-plus-react@1.5.4+51d54415590cf6f3/node_modules/motion-plus-react/dist/es/components/Cursor/use-cursor-styles.mjs
var import_react11 = __toESM(require_react(), 1);
function makeStyles() {
  const style = document.createElement("style");
  style.textContent = `
      * {
          cursor: none !important;
      }
          
      [data-motion-cursor="pointer"] {
          background-color: #333;
      }
    `;
  document.head.appendChild(style);
  return () => {
    document.head.removeChild(style);
  };
}
function useCursorStyles(isCursor) {
  (0, import_react11.useInsertionEffect)(isCursor ? makeStyles : noop, [
    isCursor
  ]);
}

// ../../node_modules/.bun/motion-plus-react@1.5.4+51d54415590cf6f3/node_modules/motion-plus-react/dist/es/components/Cursor/index.mjs
var topLeftPoint = { x: 0, y: 0 };
var centerPoint = { x: 0.5, y: 0.5 };
var skipTransition = { duration: 0 };
var defaults = {
  followSpring: { stiffness: 1e3, damping: 100 },
  magneticOptions: { morph: true, padding: 5, snap: 0.8 }
};
function Cursor({ follow = false, center = follow ? topLeftPoint : centerPoint, offset: offsetPoint = topLeftPoint, spring = follow ? defaults.followSpring : false, magnetic = false, matchTextSize = true, children, style, ...props }) {
  const shouldReduceMotion = useReducedMotion();
  useCursorStyles(!shouldReduceMotion && !follow);
  const position = usePointerPosition();
  const offset = useOffset(position, offsetPoint);
  const springX = useSpring(offset.x, spring || void 0);
  const springY = useSpring(offset.y, spring || void 0);
  const magneticOptions = typeof magnetic === "object" ? { ...defaults.magneticOptions, ...magnetic } : defaults.magneticOptions;
  const resetSpring = () => {
    const removeX = offset.x.on("change", (v) => {
      springX.jump(v);
      removeX();
    });
    const removeY = offset.y.on("change", (v) => {
      springY.jump(v);
      removeY();
    });
  };
  const state2 = useCursorState();
  const isInView = useCursorIsInView(resetSpring);
  const { x: x2, y: y2 } = useMagneticTarget(spring ? { x: springX, y: springY } : position, Boolean(magnetic), state2, magneticOptions.snap);
  const { width, height } = getCursorSize({
    type: state2.type,
    state: state2,
    hasChildren: Boolean(children),
    style,
    isMagnetic: Boolean(magnetic),
    magneticOptions,
    matchTextSize
  });
  const pointerHasMoved = useHasPointerMoved(position, resetSpring);
  return useBodyPortal(pointerHasMoved ? (0, import_jsx_runtime.jsx)(LayoutGroup, { children: (0, import_jsx_runtime.jsx)(MotionConfig, { transition: shouldReduceMotion ? skipTransition : props.transition || defaultTransition, children: (0, import_jsx_runtime.jsx)(motion.div, { layout: true, "data-motion-cursor": follow ? "follow" : "pointer", "data-framer-portal-id": "motion-cursor", initial: "exit", exit: "exit", ...props, variants: {
    pressed: follow ? {} : { scale: 0.9 },
    ...props.variants,
    default: {
      opacity: 1,
      scale: 1,
      ...props.variants?.default
    },
    exit: {
      opacity: 0,
      scale: 0,
      ...props.variants?.exit
    }
  }, animate: [
    "default",
    state2.type,
    magnetic && state2.targetBoundingBox ? "magnetic" : "",
    !isInView ? "exit" : state2.isPressed ? "pressed" : ""
  ], transformTemplate: positionTransform(center), style: {
    borderRadius: follow ? 0 : 20,
    zIndex: follow ? 99998 : 99999,
    willChange: "transform",
    contain: "layout",
    originX: center.x,
    originY: center.y,
    ...style,
    width,
    height,
    x: x2,
    y: y2,
    top: 0,
    left: 0,
    position: "fixed",
    pointerEvents: "none"
  }, children: (0, import_jsx_runtime.jsx)(AnimatePresence, { children }) }) }) }) : null);
}
function positionTransform(origin) {
  return (_, generated) => `translate(-${origin.x * 100}%, -${origin.y * 100}%) ${generated}`;
}
var defaultTransition = { duration: 0.15, ease: [0.38, 0.12, 0.29, 1] };

// ../../node_modules/.bun/motion-plus-react@1.5.4+51d54415590cf6f3/node_modules/motion-plus-react/dist/es/components/Cursor/magnetic/use-magnetic-pull.mjs
var import_react14 = __toESM(require_react(), 1);
function useOffset2(pointer, pull, target) {
  const rest = useMotionValue(0);
  const offset = useMotionValue(0);
  const crossfade = useMotionValue(0);
  (0, import_react14.useEffect)(() => {
    if (typeof target === "number") {
      animate(crossfade, 1);
      const updateOffset = () => {
        offset.set(pull * (pointer.get() - target));
      };
      updateOffset();
      return pointer.on("change", updateOffset);
    } else {
      animate(crossfade, 0);
    }
  }, [target]);
  return useTransform(() => mix(rest.get(), offset.get(), crossfade.get()));
}
function useMagneticPull(ref, pull = 0.1) {
  const state2 = useCursorState();
  const pointer = usePointerPosition();
  const isActive = (0, import_react14.useMemo)(() => {
    return state2.targetBoundingBox && state2.target === ref.current;
  }, [state2.targetBoundingBox, state2.target, ref.current]);
  const x2 = useOffset2(pointer.x, pull, isActive ? state2.targetBoundingBox.left + state2.targetBoundingBox.width / 2 : void 0);
  const y2 = useOffset2(pointer.y, pull, isActive ? state2.targetBoundingBox.top + state2.targetBoundingBox.height / 2 : void 0);
  return { x: x2, y: y2 };
}

// ../../node_modules/.bun/motion-plus-react@1.5.4+51d54415590cf6f3/node_modules/motion-plus-react/dist/es/components/AnimateNumber/index.mjs
var import_jsx_runtime6 = __toESM(require_jsx_runtime(), 1);
var import_react25 = __toESM(require_react(), 1);

// ../../node_modules/.bun/motion-plus-react@1.5.4+51d54415590cf6f3/node_modules/motion-plus-react/dist/es/components/AnimateNumber/Mask.mjs
var import_jsx_runtime2 = __toESM(require_jsx_runtime(), 1);
var maskHeight = "var(--mask-height, 0.15em)";
var maskWidth = "var(--mask-width, 0.5em)";
var correctedMaskWidth = `calc(${maskWidth} / var(--invert-x, 1))`;
var cornerGradient = `#000 0, transparent 71%`;
var mask = (
  // Horizontal:
  `linear-gradient(to right, transparent 0, #000 ${correctedMaskWidth}, #000 calc(100% - ${correctedMaskWidth}), transparent),linear-gradient(to bottom, transparent 0, #000 ${maskHeight}, #000 calc(100% - ${maskHeight}), transparent 100%),radial-gradient(at bottom right, ${cornerGradient}),radial-gradient(at bottom left, ${cornerGradient}), radial-gradient(at top left, ${cornerGradient}), radial-gradient(at top right, ${cornerGradient})`
);
var maskSize = `100% calc(100% - ${maskHeight} * 2),calc(100% - ${correctedMaskWidth} * 2) 100%,${correctedMaskWidth} ${maskHeight},${correctedMaskWidth} ${maskHeight},${correctedMaskWidth} ${maskHeight},${correctedMaskWidth} ${maskHeight}`;
addScaleCorrector({
  "--invert-x": {
    correct: (_, { treeScale, projectionDelta }) => projectionDelta.x.scale * treeScale.x
  }
});
function Mask({ children, layoutDependency }) {
  return (0, import_jsx_runtime2.jsx)(motion.span, { layout: true, layoutDependency, "aria-hidden": true, style: {
    display: "inline-flex",
    // Activates the scale correction, which gets stored in --invert-xion
    "--invert-x": 1,
    margin: `0 calc(-1*${maskWidth})`,
    padding: `calc(${maskHeight}/2) ${maskWidth}`,
    position: "relative",
    // for zIndex
    zIndex: -1,
    // should be underneath everything else
    overflow: "clip",
    // important so it doesn't affect page layout
    // Prefixed properties have better support than unprefixed ones:
    WebkitMaskImage: mask,
    WebkitMaskSize: maskSize,
    WebkitMaskPosition: "center, center, top left, top right, bottom right, bottom left",
    WebkitMaskRepeat: "no-repeat"
  }, children });
}

// ../../node_modules/.bun/motion-plus-react@1.5.4+51d54415590cf6f3/node_modules/motion-plus-react/dist/es/components/AnimateNumber/NumberSection.mjs
var import_jsx_runtime5 = __toESM(require_jsx_runtime(), 1);
var import_react23 = __toESM(require_react(), 1);

// ../../node_modules/.bun/motion-plus-react@1.5.4+51d54415590cf6f3/node_modules/motion-plus-react/dist/es/components/AnimateNumber/hooks/use-is-initial-render.mjs
var import_react16 = __toESM(require_react(), 1);
function useIsInitialRender() {
  const isInitialRender = (0, import_react16.useRef)(true);
  (0, import_react16.useEffect)(() => {
    isInitialRender.current = false;
  }, []);
  return isInitialRender.current;
}

// ../../node_modules/.bun/motion-plus-react@1.5.4+51d54415590cf6f3/node_modules/motion-plus-react/dist/es/components/AnimateNumber/NumberDigit.mjs
var import_jsx_runtime3 = __toESM(require_jsx_runtime(), 1);
var import_react18 = __toESM(require_react(), 1);

// ../../node_modules/.bun/motion-plus-react@1.5.4+51d54415590cf6f3/node_modules/motion-plus-react/dist/es/components/AnimateNumber/utils/get-width-in-ems.mjs
function getWidthInEm(element) {
  const { width, fontSize } = getComputedStyle(element);
  return `${parseFloat(width) / parseFloat(fontSize)}em`;
}

// ../../node_modules/.bun/motion-plus-react@1.5.4+51d54415590cf6f3/node_modules/motion-plus-react/dist/es/components/AnimateNumber/utils/target-widths.mjs
var targetWidths = /* @__PURE__ */ new WeakMap();

// ../../node_modules/.bun/motion-plus-react@1.5.4+51d54415590cf6f3/node_modules/motion-plus-react/dist/es/components/AnimateNumber/NumberDigit.mjs
var NumberDigit = (0, import_react18.forwardRef)(function NumberDigit2({ value: _value, initialValue: _initialValue = _value, ...rest }, _ref) {
  const { transition } = (0, import_react18.useContext)(MotionConfigContext);
  const initialValue = (0, import_react18.useRef)(_initialValue).current;
  const isInitialRender = useIsInitialRender();
  const scope = (0, import_react18.useRef)(null);
  const ref = (0, import_react18.useRef)(null);
  (0, import_react18.useImperativeHandle)(_ref, () => ref.current, []);
  const numberRefs = (0, import_react18.useRef)(new Array(10));
  const isPresent = useIsPresent();
  const value = isPresent ? _value : 0;
  (0, import_react18.useLayoutEffect)(() => {
    if (!scope.current || !numberRefs.current[initialValue])
      return;
    scope.current.style.width = getWidthInEm(numberRefs.current[initialValue]);
  }, []);
  const prevValue = (0, import_react18.useRef)(_initialValue);
  (0, import_react18.useLayoutEffect)(() => {
    if (!scope.current || value === prevValue.current)
      return;
    const box = scope.current.getBoundingClientRect();
    const refBox = ref.current?.getBoundingClientRect();
    const initialY = box.height * (value - prevValue.current) + (box.top - (refBox ? refBox.top || 0 : box.top));
    animate(scope.current, { y: [initialY, 0] }, transition);
    return () => {
      prevValue.current = value;
    };
  }, [value]);
  const [width, setWidth] = (0, import_react18.useState)();
  (0, import_react18.useEffect)(() => {
    if (isInitialRender && initialValue === value)
      return;
    if (!numberRefs.current[value])
      return;
    const w = getWidthInEm(numberRefs.current[value]);
    if (ref.current)
      targetWidths.set(ref.current, w);
    setWidth(w);
  }, [value]);
  const renderNumber = (i) => (0, import_jsx_runtime3.jsx)("span", { style: {
    display: "inline-block",
    padding: `calc(${maskHeight}/2) 0`
  }, ref: (r) => void (numberRefs.current[i] = r), children: i }, i);
  return (0, import_jsx_runtime3.jsx)(motion.span, { ...rest, ref, layout: "position", "data-state": isPresent ? void 0 : "exiting", style: {
    display: "inline-flex",
    justifyContent: "center",
    width
  }, children: (0, import_jsx_runtime3.jsxs)("span", { ref: scope, style: {
    display: "inline-flex",
    justifyContent: "center",
    flexDirection: "column",
    alignItems: "center",
    position: "relative"
  }, children: [value !== 0 && (0, import_jsx_runtime3.jsx)("span", { style: {
    ...digitFillStyle,
    bottom: `100%`,
    left: 0
  }, children: new Array(value).fill(null).map((_, i) => renderNumber(i)) }), renderNumber(value), value !== 9 && (0, import_jsx_runtime3.jsx)("span", { style: {
    ...digitFillStyle,
    top: `100%`,
    left: 0
  }, children: new Array(9 - value).fill(null).map((_, i) => renderNumber(value + i + 1)) })] }) });
});
var digitFillStyle = {
  display: "flex",
  flexDirection: "column",
  alignItems: "center",
  position: "absolute",
  width: "100%"
};

// ../../node_modules/.bun/motion-plus-react@1.5.4+51d54415590cf6f3/node_modules/motion-plus-react/dist/es/components/AnimateNumber/NumberSymbol.mjs
var import_jsx_runtime4 = __toESM(require_jsx_runtime(), 1);
var import_react21 = __toESM(require_react(), 1);

// ../../node_modules/.bun/motion-plus-react@1.5.4+51d54415590cf6f3/node_modules/motion-plus-react/dist/es/components/AnimateNumber/SectionContext.mjs
var import_react19 = __toESM(require_react(), 1);
var SectionContext = (0, import_react19.createContext)({
  justify: "left"
});

// ../../node_modules/.bun/motion-plus-react@1.5.4+51d54415590cf6f3/node_modules/motion-plus-react/dist/es/components/AnimateNumber/NumberSymbol.mjs
var NumberSymbol = (0, import_react21.forwardRef)(function NumberSymbol2({ partKey, type, children, layoutDependency, ...rest }, ref) {
  const isPresent = useIsPresent();
  const { justify } = (0, import_react21.useContext)(SectionContext);
  return (0, import_jsx_runtime4.jsx)(motion.span, { ...rest, "data-state": isPresent ? void 0 : "exiting", style: {
    display: "inline-flex",
    justifyContent: justify,
    padding: `calc(${maskHeight}/2) 0`,
    // match digits
    position: "relative"
    // needed for AnimatePresent popLayout
  }, layout: "position", layoutDependency, ref, children: (0, import_jsx_runtime4.jsx)(AnimatePresence, { mode: "popLayout", anchorX: justify, initial: false, children: (0, import_jsx_runtime4.jsx)(motion.span, { layout: justify === "right" ? "position" : false, layoutDependency, initial: { opacity: 0 }, animate: { opacity: [null, 1] }, exit: { opacity: [null, 0] }, style: {
    display: "inline-block",
    whiteSpace: "pre"
    // some symbols are spaces or thin spaces
  }, children }, children) }) });
});

// ../../node_modules/.bun/motion-plus-react@1.5.4+51d54415590cf6f3/node_modules/motion-plus-react/dist/es/components/AnimateNumber/NumberSection.mjs
var NumberSection = (0, import_react23.forwardRef)(function NumberSection2({ parts, justify = "left", mode, style, name, layoutDependency, ...rest }, _ref) {
  const ref = (0, import_react23.useRef)(null);
  (0, import_react23.useImperativeHandle)(_ref, () => ref.current, []);
  const context = (0, import_react23.useMemo)(() => ({ justify }), [justify]);
  const measuredRef = (0, import_react23.useRef)(null);
  const isInitialRender = useIsInitialRender();
  const [width, setWidth] = (0, import_react23.useState)();
  (0, import_react23.useEffect)(() => {
    if (!measuredRef.current)
      return;
    if (isInitialRender) {
      if (ref.current)
        ref.current.style.width = getWidthInEm(measuredRef.current);
      return;
    }
    const undos = Array.from(measuredRef.current.children).map((child) => {
      if (!(child instanceof HTMLElement))
        return;
      if (child.dataset.state === "exiting") {
        const next = child.nextSibling;
        child.remove();
        return () => {
          if (measuredRef.current) {
            measuredRef.current.insertBefore(child, next);
          }
        };
      }
      const newWidth = targetWidths.get(child);
      if (!newWidth)
        return;
      const oldWidth = child.style.width;
      child.style.width = newWidth;
      return () => {
        child.style.width = oldWidth;
      };
    });
    setWidth(getWidthInEm(measuredRef.current));
    for (let i = undos.length - 1; i >= 0; i--) {
      const undo = undos[i];
      if (undo)
        undo();
    }
  }, [parts.map((p) => p.value).join("")]);
  return (0, import_jsx_runtime5.jsx)(SectionContext.Provider, { value: context, children: (0, import_jsx_runtime5.jsx)(motion.span, { layoutDependency, ...rest, ref, className: `number-section-${name}`, style: {
    ...style,
    display: "inline-flex",
    justifyContent: justify,
    width
  }, children: (0, import_jsx_runtime5.jsxs)("span", { ref: measuredRef, style: {
    display: "inline-flex",
    justifyContent: "inherit",
    position: "relative"
    // needed for AnimatePresent popLayout
  }, children: ["​", (0, import_jsx_runtime5.jsx)(AnimatePresence, { mode, anchorX: justify, initial: false, children: parts.map((part) => part.type === "integer" || part.type === "fraction" ? (0, import_jsx_runtime5.jsx)(NumberDigit, { initial: { opacity: 0 }, animate: { opacity: 1 }, exit: { opacity: 0 }, value: part.value, initialValue: isInitialRender ? void 0 : 0, layoutDependency }, part.key) : (0, import_jsx_runtime5.jsx)(NumberSymbol, { type: part.type, partKey: part.key, initial: { opacity: 0 }, animate: { opacity: 1 }, exit: { opacity: 0 }, layoutDependency, children: part.value }, part.type === "literal" ? `${part.key}:${part.value}` : part.key)) })] }) }) });
});

// ../../node_modules/.bun/motion-plus-react@1.5.4+51d54415590cf6f3/node_modules/motion-plus-react/dist/es/components/AnimateNumber/utils/format-parts.mjs
var formatToParts = (value, { locales, format }, prefix, suffix) => {
  const formatter = new Intl.NumberFormat(locales, format);
  const parts = formatter.formatToParts(Number(value));
  if (prefix)
    parts.unshift({ type: "prefix", value: prefix });
  if (suffix)
    parts.push({ type: "suffix", value: suffix });
  const pre = [];
  const _integer = [];
  const fraction = [];
  const post = [];
  const counts = {};
  const generateKey = (type) => `${type}:${counts[type] = (counts[type] ?? -1) + 1}`;
  let formatted = "";
  let seenInteger = false, seenDecimal = false;
  for (const part of parts) {
    formatted += part.value;
    const type = part.type === "minusSign" || part.type === "plusSign" ? "sign" : part.type;
    switch (type) {
      case "integer":
        seenInteger = true;
        _integer.push(...part.value.split("").map((d) => ({ type, value: parseInt(d) })));
        break;
      case "group":
        _integer.push({ type, value: part.value });
        break;
      case "decimal":
        seenDecimal = true;
        fraction.push({
          type,
          value: part.value,
          key: generateKey(type)
        });
        break;
      case "fraction":
        fraction.push(...part.value.split("").map((d) => ({
          type,
          value: parseInt(d),
          key: generateKey(type)
        })));
        break;
      // case 'nan':
      // case 'infinity':
      // 	// TODO: handle these
      // 	break
      // case 'exponentSeparator':
      // 	break
      // case 'exponentMinusSign':
      // case 'exponentInteger':
      // 	break
      default:
        (seenInteger || seenDecimal ? post : pre).push({
          type,
          value: part.value,
          key: generateKey(type)
        });
    }
  }
  const integer = [];
  for (let i = _integer.length - 1; i >= 0; i--) {
    integer.unshift({
      ..._integer[i],
      key: generateKey(_integer[i].type)
    });
  }
  return { pre, integer, fraction, post, formatted };
};

// ../../node_modules/.bun/motion-plus-react@1.5.4+51d54415590cf6f3/node_modules/motion-plus-react/dist/es/components/AnimateNumber/index.mjs
var DEFAULT_TRANSITION = {
  // We use keyframes and times so the opacity/exit animations can last
  // as long as the layout animation, so Framer Motion doesn't have to
  // remove exiting elements until the layout animation is done.
  // This worked better in testing than safeToRemove() from usePresence()
  opacity: { duration: 1, ease: easeOut },
  // perceptual duration of 0.5s
  layout: { type: "spring", duration: 1, bounce: 0 },
  y: { type: "spring", duration: 1, bounce: 0 }
};
var AnimateNumber = (0, import_react25.forwardRef)(function AnimateNumber2({ children: value, locales, format, transition, style, suffix, prefix, ...rest }, ref) {
  const parts = (0, import_react25.useMemo)(() => formatToParts(value, { locales, format }, prefix, suffix), [value, locales, format]);
  const { pre, integer, fraction, post, formatted } = parts;
  const contextTransition = (0, import_react25.useContext)(MotionConfigContext).transition;
  transition = transition ?? contextTransition ?? DEFAULT_TRANSITION;
  const { layoutDependency } = rest;
  const dependency = (0, import_react25.useMemo)(() => {
    if (layoutDependency === void 0)
      return void 0;
    return { layoutDependency, value };
  }, [layoutDependency, value]);
  return (0, import_jsx_runtime6.jsx)(LayoutGroup, { children: (0, import_jsx_runtime6.jsx)(MotionConfig, { transition, children: (0, import_jsx_runtime6.jsx)(motion.div, { ...rest, ref, layout: true, layoutDependency: dependency, style: {
    lineHeight: 1,
    // make this one easy to override
    ...style,
    display: "inline-flex",
    isolation: "isolate",
    // so number can be underneath first/last
    whiteSpace: "nowrap"
  }, children: (0, import_jsx_runtime6.jsxs)(motion.div, { layout: true, layoutDependency: dependency, "aria-label": formatted, style: {
    display: "inline-flex",
    direction: "ltr",
    // I think this is needed b/c numbers are always LTR?
    isolation: "isolate",
    // so number can be underneath pre/post
    position: "relative",
    zIndex: -1
    // so the whole number is under any first/last
    // userSelect: "none", // I think adding this to the parent then undoing it on the selectable one might work a little better
    // pointerEvents: "none",
  }, children: [(0, import_jsx_runtime6.jsx)(NumberSection, { style: { padding: `calc(${maskHeight}/2) 0` }, layoutDependency: dependency, "aria-hidden": true, justify: "right", mode: "popLayout", parts: pre, name: "pre" }), (0, import_jsx_runtime6.jsxs)(Mask, { layoutDependency: dependency, children: [(0, import_jsx_runtime6.jsx)(NumberSection, { layoutDependency: dependency, justify: "right", parts: integer, name: "integer" }), (0, import_jsx_runtime6.jsx)(NumberSection, { layout: "position", layoutDependency: dependency, parts: fraction, name: "fraction" })] }), (0, import_jsx_runtime6.jsx)(NumberSection, { style: { padding: `calc(${maskHeight}/2) 0` }, "aria-hidden": true, layout: "position", layoutDependency: dependency, mode: "popLayout", parts: post, name: "post" })] }) }) }) });
});

// ../../node_modules/.bun/motion-plus-react@1.5.4+51d54415590cf6f3/node_modules/motion-plus-react/dist/es/components/AnimateText/index.mjs
var import_jsx_runtime7 = __toESM(require_jsx_runtime(), 1);
var React = __toESM(require_react(), 1);
var import_react27 = __toESM(require_react(), 1);
var MotionFragment = motion.create(React.Fragment);
function splitText(text, { splitBy = " ", charClass = "split-char", wordClass = "split-word", variants, type } = {}) {
  const words = text.split(splitBy);
  const elements = [];
  const delimiter = splitBy === " " ? " " : splitBy;
  for (let wordIndex = 0; wordIndex < words.length; wordIndex++) {
    const word = words[wordIndex];
    const characterElements = [];
    const chars = Array.from(word);
    for (let charIndex = 0; charIndex < chars.length; charIndex++) {
      let char = chars[charIndex];
      if (wordIndex < words.length - 1 && charIndex === chars.length - 1) {
        char += delimiter;
      }
      const Component = type === "char" ? motion.span : "span";
      characterElements.push((0, import_jsx_runtime7.jsx)(Component, { className: `${charClass} ${charClass}-${charIndex + 1}`, style: { display: "inline-block" }, ...type === "char" ? { variants } : {}, children: char }, `${wordIndex}-${charIndex}`));
    }
    const WordComponent = type === "word" ? motion.span : "span";
    elements.push((0, import_jsx_runtime7.jsx)(WordComponent, { className: `${wordClass} ${wordClass}-${wordIndex + 1}`, style: { display: "inline-block" }, ...type === "word" ? { variants } : {}, children: characterElements }, `word-${wordIndex}`));
  }
  return elements;
}
function AnimateText({ children, splitBy = " ", charClass = "split-char", wordClass = "split-word", lineClass = "split-line", type = "char", variants, ...props }) {
  const elements = (0, import_react27.useMemo)(() => {
    return splitText(children, {
      splitBy,
      charClass,
      wordClass,
      variants,
      type
    });
  }, [children, splitBy, charClass, wordClass]);
  return (0, import_jsx_runtime7.jsx)(MotionFragment, { ...props, children: elements });
}

// ../../node_modules/.bun/motion-plus-react@1.5.4+51d54415590cf6f3/node_modules/motion-plus-react/dist/es/components/Ticker/index.mjs
var import_jsx_runtime9 = __toESM(require_jsx_runtime(), 1);
var import_react34 = __toESM(require_react(), 1);

// ../../node_modules/.bun/motion-plus-react@1.5.4+51d54415590cf6f3/node_modules/motion-plus-react/dist/es/components/Ticker/TickerItem.mjs
var import_jsx_runtime8 = __toESM(require_jsx_runtime(), 1);
var import_react30 = __toESM(require_react(), 1);

// ../../node_modules/.bun/motion-plus-react@1.5.4+51d54415590cf6f3/node_modules/motion-plus-react/dist/es/components/Ticker/TickerItemContext.mjs
var import_react28 = __toESM(require_react(), 1);
var TickerItemContext = (0, import_react28.createContext)(void 0);

// ../../node_modules/.bun/motion-plus-react@1.5.4+51d54415590cf6f3/node_modules/motion-plus-react/dist/es/components/Ticker/TickerItem.mjs
function TickerItem({ offset, axis, listSize = 0, itemIndex, cloneIndex, bounds, inset, alignItems, itemSize = "auto", ...props }) {
  const ref = (0, import_react30.useRef)(null);
  const { start, end } = bounds;
  const transform = useTransform(() => {
    const currentOffset = offset.get();
    if (!start && !end || !listSize)
      return 0;
    if (currentOffset + end <= -inset) {
      return listSize;
    }
    return 0;
  });
  const itemOffset = useTransform(() => {
    const currentOffset = offset.get();
    const currentTransform = transform.get();
    if (!start && !end || !listSize)
      return 0;
    return currentOffset + start + currentTransform;
  });
  const itemWrapperOffAxisSize = alignItems === "stretch" ? "100%" : "fit-content";
  const isClone = cloneIndex !== void 0;
  return (0, import_jsx_runtime8.jsx)(TickerItemContext.Provider, { value: { offset: itemOffset }, children: (0, import_jsx_runtime8.jsx)(LayoutGroup, { inherit: "id", id: isClone ? `clone-${cloneIndex}` : void 0, children: (0, import_jsx_runtime8.jsx)(motion.li, { ref, ...props, className: cloneIndex === void 0 ? "ticker-item" : "clone-item", style: {
    flexGrow: 0,
    flexShrink: 0,
    flexBasis: itemSize === "fill" ? "100%" : void 0,
    display: itemSize === "fill" ? "flex" : void 0,
    height: axis === "x" ? itemWrapperOffAxisSize : void 0,
    width: axis === "y" ? itemWrapperOffAxisSize : void 0,
    x: axis === "x" ? transform : 0,
    y: axis === "y" ? transform : 0
  }, "aria-hidden": isClone ? true : void 0 }) }) });
}

// ../../node_modules/.bun/motion-plus-react@1.5.4+51d54415590cf6f3/node_modules/motion-plus-react/dist/es/components/Ticker/use-focus-navigation.mjs
var import_react32 = __toESM(require_react(), 1);
function useFocusNavigation(containerRef, axis, focusOffset, offset, setHasFocus) {
  const isFocusTrapped = (0, import_react32.useRef)(false);
  (0, import_react32.useEffect)(() => {
    const container = containerRef.current;
    if (!container)
      return;
    let detectionEnabled = false;
    const abortController = new AbortController();
    const eventOptions = {
      signal: abortController.signal
    };
    const eventOptionsWithCapture = {
      ...eventOptions,
      capture: true
    };
    const scrollProp = axis === "x" ? "scrollLeft" : "scrollTop";
    const offsetProp = axis === "x" ? "offsetLeft" : "offsetTop";
    const leftKey = axis === "x" ? "ArrowLeft" : "ArrowUp";
    const rightKey = axis === "x" ? "ArrowRight" : "ArrowDown";
    let focusableElements = [];
    let focusIndex = 0;
    const applyFocusOffset = () => {
      const nextFocusableElement = focusableElements[focusIndex];
      nextFocusableElement.focus();
      focusOffset.set(-nextFocusableElement[offsetProp]);
      container[scrollProp] = 0;
      frame.render(() => {
        container[scrollProp] = 0;
      });
    };
    const handleFocusNavigation = (event) => {
      if (event.key === "Tab") {
        event.preventDefault();
        endFocusTrap();
        const allFocusableElements = Array.from(document.querySelectorAll('a, button, input, textarea, select, [tabindex]:not([tabindex="-1"]), [contenteditable="true"]')).filter(isHTMLElement);
        allFocusableElements.sort(compareTabIndexes);
        const lastFocusableElement = allFocusableElements[event.shiftKey ? 0 : allFocusableElements.length - 1];
        const initialIndex = event.shiftKey ? allFocusableElements.length - 1 : 0;
        if (container.contains(lastFocusableElement)) {
          allFocusableElements[initialIndex].focus();
          return;
        } else {
          const indexOfCurrentElement = allFocusableElements.indexOf(focusableElements[focusIndex]);
          const delta = event.shiftKey ? -1 : 1;
          for (let i = indexOfCurrentElement; i < allFocusableElements.length && i >= 0; i += delta) {
            const element = allFocusableElements[i];
            if (!container.contains(element)) {
              element.focus();
              return;
            }
          }
        }
        return;
      } else if (event.key === leftKey) {
        focusIndex--;
      } else if (event.key === rightKey) {
        focusIndex++;
      }
      focusIndex = wrap(0, focusableElements.length, focusIndex);
      applyFocusOffset();
    };
    const startFocusTrap = () => {
      if (isFocusTrapped.current)
        return;
      setHasFocus(true);
      isFocusTrapped.current = true;
      focusableElements = Array.from(container.querySelectorAll('.ticker-item a, .ticker-item button, .ticker-item input, .ticker-item textarea, .ticker-item select, .ticker-item [tabindex]:not([tabindex="-1"]), .ticker-item [contenteditable="true"]')).filter(isHTMLElement);
      focusIndex = 0;
      applyFocusOffset();
      window.addEventListener("focus", detectTrapEnd, eventOptionsWithCapture);
      window.addEventListener("blur", detectTrapEnd, eventOptionsWithCapture);
      container.addEventListener("keydown", handleFocusNavigation, eventOptions);
    };
    const detectTrapEnd = (event) => {
      if (!event.target || !(event.target instanceof HTMLElement) || !container.contains(event.target)) {
        endFocusTrap();
      }
    };
    const endFocusTrap = () => {
      if (!isFocusTrapped.current)
        return;
      isFocusTrapped.current = false;
      setHasFocus(false);
      offset.set(focusOffset.get());
      window.removeEventListener("focus", detectTrapEnd);
      window.removeEventListener("blur", detectTrapEnd);
      container.removeEventListener("keydown", handleFocusNavigation);
    };
    const handleFocus = (event) => {
      const { target } = event;
      if (!isHTMLElement(target))
        return;
      if (!isFocusTrapped.current) {
        startFocusTrap();
      }
    };
    const detectFocusTrapEnable = () => {
      if (detectionEnabled)
        return;
      detectionEnabled = true;
      container.addEventListener("focus", handleFocus, eventOptionsWithCapture);
      window.addEventListener("pointermove", handlePointerMove, eventOptions);
    };
    const handlePointerMove = () => {
      if (!detectionEnabled)
        return;
      detectionEnabled = false;
      container.removeEventListener("focus", handleFocus, true);
      window.removeEventListener("pointermove", handlePointerMove, eventOptions);
    };
    const handleAriaHiddenClicks = (event) => {
      const target = event.target;
      let ariaHiddenAncestor = target.closest('[aria-hidden="true"]');
      if (ariaHiddenAncestor) {
        ariaHiddenAncestor.removeAttribute("aria-hidden");
      }
    };
    window.addEventListener("keydown", detectFocusTrapEnable, eventOptions);
    container.addEventListener("pointerdown", handleAriaHiddenClicks, eventOptions);
    return () => {
      abortController.abort();
      endFocusTrap();
    };
  }, []);
}
function compareTabIndexes(a, b) {
  if (a.tabIndex >= 1 && b.tabIndex >= 1) {
    return a.tabIndex - b.tabIndex;
  }
  if (a.tabIndex >= 1 && b.tabIndex <= 0) {
    return -1;
  }
  if (b.tabIndex >= 1 && a.tabIndex <= 0) {
    return 1;
  }
  return 0;
}

// ../../node_modules/.bun/motion-plus-react@1.5.4+51d54415590cf6f3/node_modules/motion-plus-react/dist/es/components/Ticker/utils/calc-total-item-length.mjs
function calcItemLength(itemPosition) {
  return itemPosition.end - itemPosition.start;
}
function calcTotalItemLength(itemPositions) {
  return itemPositions[itemPositions.length - 1].end - itemPositions[0].start;
}

// ../../node_modules/.bun/motion-plus-react@1.5.4+51d54415590cf6f3/node_modules/motion-plus-react/dist/es/components/Ticker/utils/calc-num-clones.mjs
function calcNumClones(visibleLength, itemPositions, gap) {
  const totalItemLength = calcTotalItemLength(itemPositions);
  const maxItemLength = Math.max(...itemPositions.map(calcItemLength));
  let count = 0;
  let safeFillLength = 0;
  while (safeFillLength < visibleLength) {
    safeFillLength = (totalItemLength + gap) * (count + 1) - maxItemLength;
    count++;
  }
  return Math.max(count - 1, 0);
}

// ../../node_modules/.bun/motion-plus-react@1.5.4+51d54415590cf6f3/node_modules/motion-plus-react/dist/es/components/Ticker/index.mjs
var alignAlias = {
  start: "flex-start",
  end: "flex-end"
};
function Ticker({ items, velocity = 50, hoverFactor = 1, gap = 10, axis = "x", align = "center", offset, isStatic = false, itemSize = "auto", overflow = false, ...props }) {
  const [state2, setState] = (0, import_react34.useState)({
    visibleLength: 0,
    inset: 0,
    totalItemLength: 0,
    itemPositions: []
  });
  const alignItems = alignAlias[align] || align;
  if (isStatic) {
    const renderedOffset2 = useMotionValue(0);
    return (0, import_jsx_runtime9.jsx)(ListView, { containerProps: props, gap, axis, alignItems, renderedOffset: renderedOffset2, items, itemSize, state: state2, overflow, isStatic: true });
  }
  const [hasFocus, setHasFocus] = (0, import_react34.useState)(false);
  const velocityFactor = useMotionValue(1);
  const defaultOffset = useMotionValue(0);
  offset ?? (offset = defaultOffset);
  const wrappedOffset = useTransform(() => wrap(-state2.totalItemLength - gap - state2.inset, -state2.inset, offset.get()));
  const focusOffset = useMotionValue(0);
  const renderedOffset = hasFocus ? focusOffset : wrappedOffset;
  const containerRef = (0, import_react34.useRef)(null);
  const listRef = (0, import_react34.useRef)(null);
  const isInViewport = useInView(containerRef, { margin: "100px" });
  const isPageInView = usePageInView();
  const isInView = isInViewport && isPageInView;
  const isReducedMotion = useReducedMotion();
  const updateMeasurements = () => {
    if (!containerRef.current || !listRef.current)
      return;
    const viewportLengthProp = axis === "x" ? "innerWidth" : "innerHeight";
    const lengthProp = axis === "x" ? "offsetWidth" : "offsetHeight";
    const insetProp = axis === "x" ? "offsetLeft" : "offsetTop";
    const paddingStartProp = axis === "x" ? "paddingLeft" : "paddingTop";
    const container = containerRef.current;
    const list = listRef.current;
    const allItems = list.querySelectorAll(".ticker-item");
    if (!allItems.length)
      return;
    let haveItemSizesChanged = false;
    const itemPositions = [];
    for (let i = 0; i < allItems.length; i++) {
      const item = allItems[i];
      itemPositions.push({
        start: item[insetProp],
        end: item[insetProp] + item[lengthProp]
      });
      if (!state2.itemPositions[i] || itemPositions[i].start !== state2.itemPositions[i].start || itemPositions[i].end !== state2.itemPositions[i].end) {
        haveItemSizesChanged = true;
      }
    }
    const visibleLength = overflow ? window[viewportLengthProp] : (
      /**
       * Cap to viewport size to prevent infinite or wasteful cloning in the event that the
       * container width is reactive to the number of children rendered within it.
       */
      Math.min(container[lengthProp], window[viewportLengthProp])
    );
    const totalItemLength = calcTotalItemLength(itemPositions);
    const computedContainerStyle = window.getComputedStyle(container);
    const containerPaddingStart = parseInt(computedContainerStyle[paddingStartProp] ?? 0);
    const inset = overflow ? getCumulativeOffset(allItems[0], axis) : containerPaddingStart;
    if (visibleLength !== state2.visibleLength || totalItemLength !== state2.totalItemLength || inset !== state2.inset || haveItemSizesChanged) {
      setState({
        visibleLength,
        itemPositions,
        totalItemLength,
        inset
      });
    }
  };
  useIsomorphicLayoutEffect(() => {
    if (!isInView || !containerRef.current)
      return;
    updateMeasurements();
    const trackViewport = overflow ? resize(updateMeasurements) : void 0;
    const trackContainer = resize(containerRef.current, updateMeasurements);
    return () => {
      trackViewport?.();
      trackContainer();
    };
  }, [items, isInView, overflow]);
  const isMeasured = state2.totalItemLength > 0;
  useAnimationFrame(isMeasured && isInView && offset === defaultOffset && !isReducedMotion ? (_, delta) => {
    const frameOffset = delta / 1e3 * (velocity * velocityFactor.get());
    offset.set(offset.get() - frameOffset);
  } : noop);
  const cloneCount = (0, import_react34.useMemo)(() => {
    if (!isMeasured || !state2.visibleLength)
      return 0;
    return calcNumClones(state2.visibleLength, state2.itemPositions, gap);
  }, [isMeasured, state2]);
  const totalListSize = state2.totalItemLength === 0 ? 0 : (state2.totalItemLength + gap) * (cloneCount + 1);
  const clonedItems = [];
  for (let i = 0; i < cloneCount; i++) {
    items.forEach((item, itemIndex) => {
      const originalBounds = state2.itemPositions[itemIndex];
      const cloneOffset = (state2.totalItemLength + gap) * (i + 1);
      const cloneBounds = originalBounds ? {
        start: originalBounds.start + cloneOffset,
        end: originalBounds.end + cloneOffset
      } : defaultBounds;
      clonedItems.push((0, import_jsx_runtime9.jsx)(TickerItem, { offset: renderedOffset, axis, listSize: totalListSize, cloneIndex: itemIndex, bounds: cloneBounds, inset: state2.inset, alignItems, itemSize, children: item }, `clone-${i}-${itemIndex}`));
    });
  }
  useFocusNavigation(containerRef, axis, focusOffset, offset, setHasFocus);
  return (0, import_jsx_runtime9.jsx)(ListView, { containerProps: props, containerRef, listRef, gap, axis, alignItems, isMeasured, isInView, renderedOffset, items, itemSize, clonedItems, onPointerEnter: () => {
    animate(velocityFactor, hoverFactor);
  }, onPointerLeave: () => {
    animate(velocityFactor, 1);
  }, totalListSize, state: state2, overflow });
}
function ListView({ containerProps, containerRef, listRef, gap, axis, alignItems, isMeasured, isInView, isStatic, items, clonedItems, renderedOffset, onPointerEnter, onPointerLeave, totalListSize, itemSize, overflow, state: state2 }) {
  return (0, import_jsx_runtime9.jsx)(motion.div, { ...containerProps, ref: containerRef, style: {
    ...containerStyle,
    ...containerProps.style,
    overflow: overflow ? "visible" : "hidden"
  }, onPointerEnter, onPointerLeave, children: (0, import_jsx_runtime9.jsxs)(motion.ul, { ref: listRef, style: {
    ...listStyle,
    flexDirection: axis === "x" ? "row" : "column",
    gap: `${gap}px`,
    x: axis === "x" ? renderedOffset : 0,
    y: axis === "y" ? renderedOffset : 0,
    opacity: isMeasured || isStatic ? 1 : 0,
    alignItems,
    willChange: isMeasured && isInView ? "transform" : void 0,
    width: "100%",
    height: "100%",
    maxHeight: "100%",
    maxWidth: "100%"
  }, children: [items.map((item, index) => (0, import_jsx_runtime9.jsx)(TickerItem, { axis, offset: renderedOffset, listSize: totalListSize, itemIndex: index, bounds: state2.itemPositions[index] ?? defaultBounds, inset: state2.inset, alignItems, itemSize, children: item }, "original-" + index)), clonedItems || null] }) });
}
var defaultBounds = { start: 0, end: 0 };
var containerStyle = {
  display: "flex",
  position: "relative",
  overflow: "hidden"
};
var listStyle = {
  display: "flex",
  position: "relative",
  willChange: "transform",
  listStyleType: "none",
  padding: 0,
  margin: 0,
  justifyContent: "flex-start"
};
function getCumulativeOffset(element, axis) {
  let offset = 0;
  let el = element;
  while (el) {
    offset += axis === "x" ? el.offsetLeft : el.offsetTop;
    el = el.offsetParent;
  }
  return offset;
}

// ../../node_modules/.bun/motion-plus-react@1.5.4+51d54415590cf6f3/node_modules/motion-plus-react/dist/es/components/Ticker/use-item-offset.mjs
var import_react35 = __toESM(require_react(), 1);
function useItemOffset() {
  const itemContext = (0, import_react35.useContext)(TickerItemContext);
  invariant(Boolean(itemContext), "useItemOffset must be used within a TickerItem");
  return itemContext.offset;
}

// ../../node_modules/.bun/motion-plus-react@1.5.4+51d54415590cf6f3/node_modules/motion-plus-react/dist/es/components/Typewriter/index.mjs
var import_jsx_runtime10 = __toESM(require_jsx_runtime(), 1);

// ../../node_modules/.bun/motion-plus-dom@1.6.0+51d54415590cf6f3/node_modules/motion-plus-dom/dist/es/typewriter/needs-backspace.mjs
function needsBackspace(currentText, fullText) {
  return currentText.length > fullText.length || currentText.length > 0 && !fullText.startsWith(currentText);
}

// ../../node_modules/.bun/motion-plus-dom@1.6.0+51d54415590cf6f3/node_modules/motion-plus-dom/dist/es/typewriter/delay.mjs
function mix2(a, b, t) {
  return a + (b - a) * t;
}
function getTypewriterDelay(fullText, currentText, interval, variance, backspaceFactor) {
  if (needsBackspace(currentText, fullText)) {
    return interval * backspaceFactor;
  }
  if (variance === "natural") {
    return getNaturalDelay(fullText, currentText, interval);
  }
  if (typeof variance === "number" && variance > 0) {
    const varianceAmount = interval * (variance / 100);
    return interval + mix2(-varianceAmount, varianceAmount, Math.random());
  }
  return interval;
}
function getNaturalDelay(fullText, currentText, interval) {
  const currentIndex = currentText.length;
  const char = fullText[currentIndex];
  const previousChar = fullText[currentIndex - 1];
  if (!char)
    return interval;
  const beforeText = fullText.slice(0, currentIndex);
  const lastSpaceIndex = beforeText.lastIndexOf(" ");
  const positionInWord = currentIndex - lastSpaceIndex - 1;
  const wordStart = lastSpaceIndex + 1;
  const afterCurrentIndex = fullText.slice(currentIndex);
  const nextSpaceIndex = afterCurrentIndex.indexOf(" ");
  const wordEnd = nextSpaceIndex === -1 ? fullText.length : currentIndex + nextSpaceIndex;
  const wordLength = wordEnd - wordStart;
  let delayMultiplier = 1;
  if (previousChar && /[.!?]/.test(previousChar) && char === " ") {
    delayMultiplier *= 3;
  }
  if (wordLength <= 3) {
    delayMultiplier *= 0.7;
  } else {
    if (positionInWord === 0 && char !== " ") {
      delayMultiplier *= 1.5;
    }
    if (positionInWord === wordLength - 1) {
      delayMultiplier *= 1.4;
    }
  }
  if (positionInWord > 0 && positionInWord < wordLength - 1 && wordLength > 3) {
    const middleBoost = Math.min(positionInWord / wordLength, 0.4);
    delayMultiplier *= 1 - middleBoost;
  }
  if (punctuation.has(char)) {
    delayMultiplier *= 1.5;
  }
  if (shiftRequired.has(char)) {
    delayMultiplier *= 1.5;
  }
  if (/\d/.test(char)) {
    delayMultiplier *= 1.3;
  }
  if (wordLength > 8) {
    delayMultiplier *= 1.3;
  }
  if (char !== char.toLowerCase()) {
    delayMultiplier *= 1.25;
  }
  const fatigueThreshold = 200;
  if (currentIndex > fatigueThreshold) {
    const fatigueAmount = Math.min((currentIndex - fatigueThreshold) / 1e3, 0.3);
    delayMultiplier *= 1 + fatigueAmount;
  }
  const randomVariance = mix2(-0.25, 0.25, Math.random());
  delayMultiplier *= 1 + randomVariance;
  const finalDelay = interval * delayMultiplier;
  return Math.max(interval * 0.2, finalDelay);
}
var punctuation = /* @__PURE__ */ new Set([
  ".",
  ",",
  "!",
  "?",
  ":",
  ";",
  "'",
  '"',
  "-",
  "(",
  ")"
]);
var shiftRequired = /* @__PURE__ */ new Set([
  "!",
  "@",
  "#",
  "$",
  "%",
  "^",
  "&",
  "*",
  "(",
  ")",
  "_",
  "+",
  "{",
  "}",
  "|",
  ":",
  '"',
  "<",
  ">",
  "?"
]);

// ../../node_modules/.bun/motion-plus-dom@1.6.0+51d54415590cf6f3/node_modules/motion-plus-dom/dist/es/typewriter/find-previous-word-index.mjs
function findPreviousWordIndex(text, fromIndex) {
  let i = fromIndex - 1;
  while (i >= 0 && /\s/.test(text[i])) {
    i--;
  }
  while (i >= 0 && !/\s/.test(text[i])) {
    i--;
  }
  return Math.max(0, i + 1);
}

// ../../node_modules/.bun/motion-plus-dom@1.6.0+51d54415590cf6f3/node_modules/motion-plus-dom/dist/es/typewriter/find-common-prefix-index.mjs
function findCommonPrefixIndex(current, target) {
  const commonPrefixLength = Math.min(current.length, target.length);
  let prefixLength = 0;
  for (let i = 0; i < commonPrefixLength; i++) {
    if (current[i] === target[i]) {
      prefixLength = i + 1;
    } else {
      break;
    }
  }
  return prefixLength;
}

// ../../node_modules/.bun/motion-plus-dom@1.6.0+51d54415590cf6f3/node_modules/motion-plus-dom/dist/es/typewriter/get-next-text.mjs
function getNextText(current, target, replace, backspace) {
  if (replace === "type" && needsBackspace(current, target)) {
    if (backspace === "all") {
      return target.slice(0, findCommonPrefixIndex(current, target));
    } else if (backspace === "word") {
      const newLength = findPreviousWordIndex(current, current.length);
      return current.slice(0, newLength);
    } else {
      return current.slice(0, -1);
    }
  }
  return target.slice(0, current.length + 1);
}

// ../../node_modules/.bun/motion-plus-react@1.5.4+51d54415590cf6f3/node_modules/motion-plus-react/dist/es/components/Typewriter/index.mjs
var import_react37 = __toESM(require_react(), 1);

// ../../node_modules/.bun/motion-plus-react@1.5.4+51d54415590cf6f3/node_modules/motion-plus-react/dist/es/components/Typewriter/types.mjs
var TYPING_SPEEDS = {
  slow: 130,
  normal: 75,
  fast: 30
};

// ../../node_modules/.bun/motion-plus-react@1.5.4+51d54415590cf6f3/node_modules/motion-plus-react/dist/es/components/Typewriter/index.mjs
var Typewriter = (0, import_react37.forwardRef)(function Typewriter2({ children: text = "", as, speed = "normal", variance = "natural", cursorClassName = "motion-typewriter-cursor", cursorStyle, cursorBlinkDuration = 0.5, onComplete, play = true, "aria-label": ariaLabel, textClassName, textStyle, replace = "type", backspace = "character", backspaceFactor = 0.2, ...props }, ref) {
  const Component = as || "span";
  const targetText = (0, import_react37.useRef)(text);
  const displayText = useMotionValue("");
  const cancelDelay = (0, import_react37.useRef)(null);
  const cursorBlinkAnimation = (0, import_react37.useRef)(null);
  const cursorRef = (0, import_react37.useRef)(null);
  const interval = typeof speed === "number" ? speed : TYPING_SPEEDS[speed];
  const clearDelay = () => {
    cancelDelay.current?.();
    cancelDelay.current = null;
  };
  const startCursorBlinkAnimation = () => {
    cursorBlinkAnimation.current = animate(cursorRef.current, {
      opacity: [1, 1, 0, 0]
    }, {
      duration: cursorBlinkDuration,
      times: [0, 0.5, 0.5, 1],
      ease: "linear",
      repeat: Infinity,
      repeatType: "reverse"
    });
  };
  (0, import_react37.useEffect)(() => {
    if (replace === "all" && text !== targetText.current) {
      displayText.set("");
    }
    targetText.current = text;
  }, [text, replace]);
  (0, import_react37.useEffect)(() => {
    if (!play) {
      startCursorBlinkAnimation();
      clearDelay();
      return;
    }
    cursorBlinkAnimation.current?.cancel();
    const nextCharacter = () => {
      const nextText = getNextText(displayText.get(), text, replace, backspace);
      displayText.set(nextText);
      if (nextText !== text) {
        scheduleNextCharacter();
      } else {
        startCursorBlinkAnimation();
        onComplete?.();
      }
    };
    const scheduleNextCharacter = () => {
      cancelDelay.current = delay(nextCharacter, getTypewriterDelay(text, displayText.get(), interval, variance, backspaceFactor));
    };
    if (!cancelDelay.current) {
      scheduleNextCharacter();
    }
    return clearDelay;
  }, [play, onComplete, text, interval, variance, backspaceFactor, backspace]);
  return (0, import_jsx_runtime10.jsxs)(Component, { ref, ...props, "aria-label": ariaLabel || text, children: [(0, import_jsx_runtime10.jsx)(motion.span, { className: textClassName, style: textStyle, children: displayText }), (0, import_jsx_runtime10.jsx)(motion.span, { ref: cursorRef, className: cursorClassName, style: {
    display: "inline-block",
    width: "2px",
    height: "1em",
    backgroundColor: "currentColor",
    position: "relative",
    top: "0.1em",
    left: "0.2em",
    ...cursorStyle
  } })] });
});
export {
  AnimateNumber,
  AnimateText,
  Cursor,
  Ticker,
  Typewriter,
  useCursorIsInView,
  useCursorState,
  useItemOffset,
  useMagneticPull,
  usePointerPosition
};
//# sourceMappingURL=motion-plus_react.js.map
