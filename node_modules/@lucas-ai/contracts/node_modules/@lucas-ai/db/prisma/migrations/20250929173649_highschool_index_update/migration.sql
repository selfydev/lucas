/*
  Warnings:

  - A unique constraint covering the columns `[externalId]` on the table `HighSchool` will be added. If there are existing duplicate values, this will fail.

*/
-- AlterTable
ALTER TABLE "public"."Address" ADD COLUMN     "highSchoolId" TEXT;

-- AlterTable
ALTER TABLE "public"."HighSchool" ADD COLUMN     "externalId" TEXT;

-- CreateIndex
CREATE INDEX "Address_highSchoolId_idx" ON "public"."Address"("highSchoolId");

-- CreateIndex
CREATE UNIQUE INDEX "HighSchool_externalId_key" ON "public"."HighSchool"("externalId");

-- AddForeignKey
ALTER TABLE "public"."Address" ADD CONSTRAINT "Address_highSchoolId_fkey" FOREIGN KEY ("highSchoolId") REFERENCES "public"."HighSchool"("id") ON DELETE CASCADE ON UPDATE CASCADE;
