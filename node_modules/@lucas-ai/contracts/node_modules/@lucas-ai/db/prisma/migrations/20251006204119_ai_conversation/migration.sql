-- CreateTable
CREATE TABLE "public"."AIConversation" (
    "id" TEXT NOT NULL,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,
    "userId" TEXT NOT NULL,
    "type" TEXT NOT NULL,

    CONSTRAINT "AIConversation_pkey" PRIMARY KEY ("id")
);

-- CreateTable
CREATE TABLE "public"."AIMessage" (
    "id" TEXT NOT NULL,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "conversationId" TEXT NOT NULL,
    "role" TEXT NOT NULL,

    CONSTRAINT "AIMessage_pkey" PRIMARY KEY ("id")
);

-- CreateTable
CREATE TABLE "public"."AIMessagePart" (
    "id" TEXT NOT NULL,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "messageId" TEXT NOT NULL,
    "type" TEXT NOT NULL,
    "order" INTEGER NOT NULL,
    "text_content" TEXT,
    "tool_callId" TEXT,
    "tool_name" TEXT,
    "tool_input" JSONB,
    "tool_result" JSONB,
    "data_type" TEXT,
    "data_content" JSONB,

    CONSTRAINT "AIMessagePart_pkey" PRIMARY KEY ("id")
);

-- CreateIndex
CREATE INDEX "AIConversation_userId_idx" ON "public"."AIConversation"("userId");

-- CreateIndex
CREATE INDEX "AIConversation_type_idx" ON "public"."AIConversation"("type");

-- CreateIndex
CREATE UNIQUE INDEX "AIConversation_userId_type_key" ON "public"."AIConversation"("userId", "type");

-- CreateIndex
CREATE INDEX "AIMessage_conversationId_idx" ON "public"."AIMessage"("conversationId");

-- CreateIndex
CREATE INDEX "AIMessage_createdAt_idx" ON "public"."AIMessage"("createdAt");

-- CreateIndex
CREATE INDEX "AIMessagePart_messageId_idx" ON "public"."AIMessagePart"("messageId");

-- CreateIndex
CREATE INDEX "AIMessagePart_type_idx" ON "public"."AIMessagePart"("type");

-- CreateIndex
CREATE UNIQUE INDEX "AIMessagePart_messageId_order_key" ON "public"."AIMessagePart"("messageId", "order");

-- AddForeignKey
ALTER TABLE "public"."AIMessage" ADD CONSTRAINT "AIMessage_conversationId_fkey" FOREIGN KEY ("conversationId") REFERENCES "public"."AIConversation"("id") ON DELETE CASCADE ON UPDATE CASCADE;

-- AddForeignKey
ALTER TABLE "public"."AIMessagePart" ADD CONSTRAINT "AIMessagePart_messageId_fkey" FOREIGN KEY ("messageId") REFERENCES "public"."AIMessage"("id") ON DELETE CASCADE ON UPDATE CASCADE;
