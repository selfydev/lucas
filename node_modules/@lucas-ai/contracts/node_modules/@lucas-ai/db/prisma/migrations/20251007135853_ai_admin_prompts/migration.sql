-- CreateTable
CREATE TABLE "public"."InterviewPrompt" (
    "id" TEXT NOT NULL,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,
    "name" TEXT NOT NULL,
    "description" TEXT,
    "promptText" TEXT NOT NULL,
    "interviewType" TEXT NOT NULL DEFAULT 'extracurricular',
    "isActive" BOOLEAN NOT NULL DEFAULT false,
    "createdBy" TEXT NOT NULL,

    CONSTRAINT "InterviewPrompt_pkey" PRIMARY KEY ("id")
);

-- CreateIndex
CREATE INDEX "InterviewPrompt_interviewType_isActive_idx" ON "public"."InterviewPrompt"("interviewType", "isActive");

-- CreateIndex
CREATE INDEX "InterviewPrompt_createdBy_idx" ON "public"."InterviewPrompt"("createdBy");

-- CreateIndex
CREATE INDEX "InterviewPrompt_createdAt_idx" ON "public"."InterviewPrompt"("createdAt" DESC);

-- AddForeignKey
ALTER TABLE "public"."InterviewPrompt" ADD CONSTRAINT "InterviewPrompt_createdBy_fkey" FOREIGN KEY ("createdBy") REFERENCES "public"."User"("id") ON DELETE CASCADE ON UPDATE CASCADE;
