-- CreateTable
CREATE TABLE "StudentCollegeList" (
    "studentId" TEXT NOT NULL,
    "universityId" INTEGER NOT NULL,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "tags" TEXT[],
    "notes" TEXT,

    CONSTRAINT "StudentCollegeList_pkey" PRIMARY KEY ("studentId","universityId")
);

-- CreateIndex
CREATE INDEX "StudentCollegeList_universityId_idx" ON "StudentCollegeList"("universityId");

-- CreateIndex
CREATE INDEX "StudentCollegeList_createdAt_idx" ON "StudentCollegeList"("createdAt");

-- AddForeignKey
ALTER TABLE "StudentCollegeList" ADD CONSTRAINT "StudentCollegeList_studentId_fkey" FOREIGN KEY ("studentId") REFERENCES "StudentProfile"("id") ON DELETE CASCADE ON UPDATE CASCADE;

-- AddForeignKey
ALTER TABLE "StudentCollegeList" ADD CONSTRAINT "StudentCollegeList_universityId_fkey" FOREIGN KEY ("universityId") REFERENCES "University"("unitid") ON DELETE CASCADE ON UPDATE CASCADE;
