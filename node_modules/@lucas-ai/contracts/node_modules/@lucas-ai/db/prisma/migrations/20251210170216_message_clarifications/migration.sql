-- CreateEnum
CREATE TYPE "MessagingClarificationStatus" AS ENUM ('PENDING', 'ANSWERED');

-- CreateTable
CREATE TABLE "MessagingClarification" (
    "id" TEXT NOT NULL,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,
    "conversationId" TEXT NOT NULL,
    "askedMessageId" TEXT NOT NULL,
    "status" "MessagingClarificationStatus" NOT NULL DEFAULT 'PENDING',
    "clarificationHint" TEXT,

    CONSTRAINT "MessagingClarification_pkey" PRIMARY KEY ("id")
);

-- CreateIndex
CREATE INDEX "MessagingClarification_conversationId_idx" ON "MessagingClarification"("conversationId");

-- CreateIndex
CREATE INDEX "MessagingClarification_status_idx" ON "MessagingClarification"("status");

-- AddForeignKey
ALTER TABLE "MessagingClarification" ADD CONSTRAINT "MessagingClarification_conversationId_fkey" FOREIGN KEY ("conversationId") REFERENCES "MessagingConversation"("id") ON DELETE CASCADE ON UPDATE CASCADE;

-- AddForeignKey
ALTER TABLE "MessagingClarification" ADD CONSTRAINT "MessagingClarification_askedMessageId_fkey" FOREIGN KEY ("askedMessageId") REFERENCES "MessagingMessage"("id") ON DELETE CASCADE ON UPDATE CASCADE;
